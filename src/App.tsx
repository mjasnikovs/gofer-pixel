/** The dev playground. Nothing is built yet — see `docs/techstack.md`. */
export const App = () => (
    <main style={{fontFamily: 'system-ui', padding: 32, color: '#ddd', background: '#111'}}>
        <h1>gofer-pixel</h1>
        <p>Rebuilding against docs/techstack.md. Nothing here yet.</p>
    </main>
)
